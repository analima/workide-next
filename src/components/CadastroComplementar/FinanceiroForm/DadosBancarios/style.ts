import { AZUL } from './../../../../styles/variaveis';
import styled from 'styled-components';

const Content = styled.div``;

export const InfoMessgae = styled.span`
  color: ${AZUL};
  font-size: 16px;
  margin-top: 4px;
`;

export default Content;
